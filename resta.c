#include <stdio.h>

int main(){
		
	int num1, num2, residuo;
		
	printf("Ingresa los numeros que deseas restar: \n");
	scanf("%d %d", &num1, &num2);
	
	residuo = num1 - num2;
	
	if(num1, num2 % 5 == 0)
		
		printf("El resultado de la resta es: %d", residuo);

	else
		
		printf("No es impar");
	
	return 0;
}
